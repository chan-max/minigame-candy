
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'candy.1s.design',
  appName: '糖果消消乐',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
